'use strict';

const path = require('path');

module.exports = {
  config: path.resolve(path.join(process.cwd(), 'src/sequilizedir'), 'config.js'),
  'seeders-path': path.resolve(path.join(process.cwd(), 'src/sequilizedir'), 'seeders'),
  'migrations-path': path.resolve(path.join(process.cwd(), 'src/sequilizedir'), 'migrations'),
};
